// 单行输入
// let input = read_line();

// 读取输入的行数
let n = read_line();
while (n--) {
  var input = read_line();
}

// 数组
let len = read_line();
let arr = [];

arr = read_line().split(" ");
for (let i = 0; i < arr.length; i++) {
  arr[i] = parseInt(arr[i]);
}
